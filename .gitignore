# Secrets -- never commit, anywhere in the tree
.env
**/.env

# Python
__pycache__/
*.py[oc]
*.egg-info
.venv/
.pytest_cache/

# Claude Code local session data
.claude/

# Office temp/lock files (e.g. ~$Rotpunkt.pptx)
~$*

# OS
.DS_Store
Thumbs.db

# Work-in-progress presentation drafts, not ready to share yet
Presentation/

# Internal notes used to generate the synthetic dataset, not for sharing
dataset/dataset_brief_for_claude_chat.md
