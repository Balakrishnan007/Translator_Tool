# Secrets -- never commit, anywhere in the tree
.env
**/.env

# Python
__pycache__/
*.py[oc]
*.egg-info
.venv/
.pytest_cache/

# Claude Code local session data
.claude/

# Office temp/lock files (e.g. ~$Rotpunkt.pptx)
~$*

# OS
.DS_Store
Thumbs.db

# Work-in-progress presentation drafts, not ready to share yet
Presentation/
